version = '1.0'
project.ext {
    type = "server"
}

description = ""

apply plugin: 'application'

dependencies {
    implementation project(':kronos-server')
}

application {
    mainClassName = 'io.ruin.central.Server'
}
